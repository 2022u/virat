use strict; 
use warnings; 
use 5.010; 
 
use Email::Address; 
print "enter email"; 
my $line = <STDIN> ; 
chomp($line); 
 
my $addresses = Email::Address->parse($line); 
print $addresses,"\n"; 
if($addresses==1) 
{ 
print "valied email address"; 
} 
else{ 
print "invalied email address"; 